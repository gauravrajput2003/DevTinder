// Membership amounts in INR (rupees)
// Used for Razorpay order creation (convert to paise by * 100)
const memberShipAmount = {
    silver: 30,
    gold: 50,
};

module.exports = { memberShipAmount };